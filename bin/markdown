#!/usr/bin/env node

var marked = require("marked")
    pygmentize = require("pygmentize-bundled");

process.stdin.resume();
process.stdin.setEncoding("utf8");

process.stdin.on("data", function (chunk) {
    console.log(marked(chunk));
});

